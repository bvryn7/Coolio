import React from 'react';
import { Card } from '@mantine/core';

// Cleaned component for future use
const WhiteCardTwo: React.FC = () => {
  return (
    <Card
      padding="lg"
      style={{
        borderRadius: '20px',
        boxShadow: '0 4px 8px rgba(0, 0, 0, 0.1)',
        maxWidth: '1000px',
        margin: '0 auto',
        backgroundColor: '#e0f7fa', // Light blue background color
      }}
    >
      {/* Add any necessary content here or keep empty */}
    </Card>
  );
};

export default WhiteCardTwo;